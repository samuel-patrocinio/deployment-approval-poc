apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: frontend
  namespace: apps
spec:
  values:
    image:
      repository: ghcr.io/samuel-patrocinio/frontend
      tag: "0.0.1-qa"
