apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: scheduler
  namespace: apps
spec:
  values:
    image:
      repository: ghcr.io/samuel-patrocinio/scheduler
      tag: "0.0.1-prod"
